node_modules
dist
dist-types
build
*.tsbuildinfo
*.tgz
/release-artifacts/

.DS_Store
.env
.env.local
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
*.pfx
.npmrc
.netrc
credentials.json
service-account*.json
.aws/
.gcp/

# Velloo design folder caches
**/.design/cache/

# IDE
.vscode/*
!.vscode/extensions.json
.idea

# Throwaway drive scripts for manual canvas verification
.scratch/

# Local agent worktree checkouts — nested repos, never part of a commit
.claude/worktrees/
.claude/settings.local.json

# Editor / agent MCP wiring is per-machine — `velloo connect` writes it.
.mcp.json
.cursor/
