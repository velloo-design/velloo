/**
 * Sprint A: dormant. Board-level free notes still load/persist but
 * aren't rendered until Sprint D wires the Board's coordinate system
 * up to a notes overlay.
 */
export function NotesLayer(): null {
  return null;
}
