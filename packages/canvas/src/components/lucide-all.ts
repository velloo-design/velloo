// The full icon namespace, reached only through `lucide-glyphs.ts`'s dynamic
// import. A module of its own so the bundler can split it away from the named
// icon imports the rest of the chrome makes from the same package.
export * from "lucide-react";
