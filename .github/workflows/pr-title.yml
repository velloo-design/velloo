# The PR title becomes the squash-merge commit subject, which is what the
# release notes are built from — so it has to be a Conventional Commits subject.
name: PR title

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read

concurrency:
  group: pr-title-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  pr-title:
    name: PR title
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.4.2

      # The title is passed through the environment, never interpolated into
      # the script, so a crafted title can't run as shell.
      - name: Check the title
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: bun scripts/check-pr-title.ts
